package main

import (
	"testing"
)

func TestIsSNumber(t *testing.T) {
	// examples := []struct {
	// 	num    int
	// 	expect bool
	// }{
	// 	{num: 9, expect: true},
	// 	{num: 10, expect: false},
	// }

	// for _, tt := range examples {
	// 	t.Run(fmt.Sprintf("Evaluating %d", tt.num), func(t *testing.T) {
	// 		got := isSNumber(tt.num)
	// 		want := tt.expect

	// 		if got != want {
	// 			t.Errorf("Got %t, wanted %t", got, want)
	// 		}
	// 	})
	// }
}

func TestGetCombos(t *testing.T) {
	// got := len(GetCombos("12345"))
	// want := 14

	// if got != want {
	// 	t.Errorf("got %d, want %d\n", got, want)
	// }
}
